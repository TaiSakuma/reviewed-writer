repos:
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.1.0
    hooks:
      - id: prettier
        types: [markdown]
        # CHANGELOG.md is CI-generated; prettier would rewrap it
        exclude: ^CHANGELOG\.md$
